manifest {
  nextflowVersion = '!>=20.12.1-edge'
}

// INSERT CUSTOM IMPORTS HERE
includeConfig "./normalize/nextflow.config"
// END INSERT

docker {
  runOptions = "-v \$(pwd):\$(pwd)"
}

process {
  errorStrategy = 'ignore'
  container = 'nextflow/bash:latest'
}
